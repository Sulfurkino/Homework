# Итоговое тестирование по курсу «Java Core» (блоки 01–17)

## Как проходить

- **Всего баллов:** 255
- **Время:** 180 минут. Можно разбить на две сессии: части 1–5 (техминимум) и части 6–9 (практика).
- **Без IDE и без интернета.** Код нигде не запускаем, отвечаем «на глаз» — в этом весь смысл.
- Ответы пиши прямо в этом файле или в отдельном документе, указывая номера заданий.
- Если не знаешь ответ — не угадывай молча, а напиши, какая часть непонятна. Это полезнее для разбора, чем случайная буква.

## Структура

| Часть | Тип заданий | Заданий | Баллов |
|-------|-------------|---------|--------|
| 1 | Один вариант ответа | 60 | 60 |
| 2 | Несколько вариантов ответа | 9 | 18 |
| 3 | Верно / неверно | 16 | 16 |
| 4 | Соответствия | 5 | 20 |
| 5 | Впиши пропущенное | 22 | 22 |
| 6 | Что выведет код | 18 | 36 |
| 7 | Найди и исправь ошибку | 8 | 24 |
| 8 | Развёрнутый ответ | 8 | 24 |
| 9 | Напиши код | 7 | 35 |
| | **Итого** | **153** | **255** |

---

# Часть 1. Один вариант ответа

По 1 баллу за верный ответ. Выбери **одну** букву.

### 1.1

Что делает компилятор `javac`?

- A. Запускает байт-код на виртуальной машине
- 🟢 B. Превращает `.java` в `.class` (байт-код)
- C. Превращает `.java` сразу в машинный код процессора
- D. Оптимизирует «горячие» участки кода во время выполнения

### 1.2

Что такое JDK?

- A. Только виртуальная машина
- B. JVM + стандартные библиотеки
-🟢 C. JRE + инструменты разработки (компилятор, отладчик, javadoc)
- D. Среда разработки IntelliJ IDEA

### 1.3

Сколько в Java примитивных типов?

- A. 6
- B. 7
- 🟢C. 8
- D. 9

### 1.4

Что выведет `System.out.println(7 / 2);`?

- A. 3.5
- 🟢B. 3
- C. 4
- D. Ошибка компиляции

### 1.5

Что произойдёт с этой строкой?

```java
long distance = 10_000_000_000;
```

- A. Скомпилируется, значение корректное
- 🟢B. Ошибка компиляции: целый литерал по умолчанию имеет тип `int`
- C. Скомпилируется, но в runtime будет переполнение
- D. Значение станет 0

### 1.6

Чему равны элементы массива после `new String[3]`?

- A. Пустым строкам `""`
- 🟢B. `null`
- C. Строкам `"null"`
- D. Неопределённому «мусору» из памяти

### 1.7

Как правильно получить размер массива `arr` и длину строки `s`?

- A. `arr.length()` и `s.length()`
- B. `arr.length` и `s.length`
- 🟢C. `arr.length` и `s.length()`
- D. `arr.size()` и `s.size()`

### 1.8

Что выведет код?

```java
String s = "Hello";
s.toUpperCase();
System.out.println(s);
```

- A. HELLO
-🟢 B. Hello
- C. null
- D. Ошибка компиляции

### 1.9

Что вернут `a == b` и `a.equals(b)`?

```java
String a = "Java";
String b = new String("Java");
```

- A. true / true
- B. false / true
- 🟢C. true / false
- D. false / false

### 1.10

Чему будут равны `a` и `b`?

```java
int a = 5;
int b = a++;
```

- 🟢A. b = 5, a = 6
- B. b = 6, a = 6
- C. b = 6, a = 5
- D. b = 5, a = 5

### 1.11

Что произойдёт, если в классическом `switch` забыть `break`?

- A. Ошибка компиляции
- B. Выполнится только текущий `case`
- 🟢C. Выполнение «провалится» в следующие `case` (fall-through)
- D. Сразу выполнится `default`

### 1.12

Как в Java передаются аргументы в метод?

- A. Примитивы — по значению, объекты — по ссылке
- 🟢B. Всегда по значению: копируется либо значение примитива, либо значение ссылки
- C. Всегда по ссылке
- D. Зависит от того, помечен ли параметр как `final`

### 1.13

Перегрузка (overloading) методов допустима, если методы отличаются:

- A. Только именами параметров
- B. Только возвращаемым типом
- 🟢C. Количеством, типами или порядком типов параметров
- D. Только модификатором доступа

### 1.14

Что делает `Arrays.sort(arr)`?

- A. Возвращает новый отсортированный массив, оригинал не меняет
-🟢 B. Сортирует переданный массив «на месте»
- C. Сортирует только массивы объектов
- D. Возвращает отсортированный `List`

### 1.15

`Arrays.binarySearch(arr, x)` вернёт корректный результат только если:

- A. Массив не содержит дубликатов
- B. Массив отсортирован
- C. Массив состоит из объектов
- D. Длина массива — степень двойки

### 1.16

Что выведет `System.out.println(arr)` для `int[] arr = {1, 2, 3}`?

- A. `[1, 2, 3]`
- B. `123`
- 🟢C. Что-то вида `[I@1b6d3586`
- D. Ошибка компиляции

### 1.17

Класс, в котором не объявлено ни одного конструктора:

- A. Не скомпилируется
- 🟢B. Получает неявный конструктор без параметров
- C. Нельзя создать через `new`
- D. Получает конструктор, копирующий все поля

### 1.18

Вызов `this(...)` внутри конструктора:

- A. Запрещён
-🟢 B. Должен быть первой строкой конструктора
- C. Должен быть последней строкой конструктора
- D. Может стоять в любом месте

### 1.19

Что НЕ может делать статический метод?

- A. Принимать параметры
- B. 🟢 Обращаться к нестатическим полям и `this`
- C. Возвращать значение
- D. Вызывать другие статические методы

### 1.20

От скольких классов может наследоваться один класс в Java?

- A.🟢  От одного
- B. От двух
- C. От любого количества
- D. Ни от одного, только реализовывать интерфейсы

### 1.21

Какой модификатор даёт доступ внутри класса, внутри пакета и в наследниках, но не всем остальным?

- A. `private`
- B. 🟢 `protected`
- C. `public`
- D. Модификатор по умолчанию (package-private)

### 1.22

Что верно про абстрактный класс?

- A. Его нельзя создать через `new`
- B. Он не может иметь конструктор
- C. 🟢 В нём не может быть обычных, не абстрактных методов
- D. От него можно наследоваться только один раз во всей программе

### 1.23

В какой версии Java в интерфейсах появились `default`-методы?

- A. Java 7
- B. Java 8
- C. Java 11
- D. Java 14

### 1.24

Если `a.equals(b)` возвращает `true`, то:

- A. 🟢`a.hashCode()` обязан быть равен `b.hashCode()`
- B. `a == b` тоже обязательно `true`
- C.  `hashCode()` может быть любым, это не связано
- D. `hashCode()` обязан различаться

### 1.25

Что верно про `record`?

- A. От него можно наследоваться другим классам
- B. 🟢 Он неявно `final`, наследоваться от него нельзя
- C. Его поля можно менять через сеттеры
- D. В его тело можно добавить новое поле состояния

### 1.26

Какие исключения компилятор заставляет обработать (`try-catch` или `throws`)?

- A. Все, унаследованные от `RuntimeException`
- 🟢 B. Checked — то есть наследники `Exception`, кроме `RuntimeException`
- C. Только наследники `Error`
- D. Никакие, обработка всегда добровольная

### 1.27

В каком порядке должны идти блоки `catch`?

- A. Сначала более общие, потом более конкретные
- 🟢 B. Сначала более конкретные, потом более общие
- C. Порядок не имеет значения
- D. Только в алфавитном порядке имён исключений

### 1.28

Блок `finally`:

- A. Выполнится только если исключения не было
- B. Выполнится только если исключение было
- 🟢 C. Выполнится всегда, в том числе если в `try` был `return`
- D. Не выполнится, если в `try` был `return`

### 1.29

Какой интерфейс должен реализовывать объект, чтобы его можно было использовать в try-with-resources?

- A. `Closeable` и только он
- B. `AutoCloseable`
- C. `Serializable`
- D. `Iterable`

### 1.30

Какова начальная ёмкость `ArrayList` по умолчанию?

- A. 0
- B. 8
- C. 10
- D. 16

### 1.31

Какова сложность `get(index)` у `ArrayList` и у `LinkedList`?

- A. O(1) и O(1)
- B. O(1) и O(n)
- 🟢 C. O(n) и O(1)
- D. O(log n) и O(n)

### 1.32

Какая коллекция всегда хранит элементы в отсортированном виде?

- A. `HashSet`
- B. `LinkedHashSet`
- 🟢C. `TreeSet`
- D. `ArrayList`

### 1.33

Что произойдёт при выполнении кода?

```java
List<String> list = List.of("A", "B");
list.add("C");
```

- A. В списке станет три элемента
- B. `UnsupportedOperationException`
- 🟢C. Ошибка компиляции
- D. `IllegalStateException`

### 1.34

К чему обычно приводит удаление элемента из коллекции внутри `for-each`?

- A. Ничего не произойдёт, элемент просто удалится
- B. `ConcurrentModificationException`
- C. `UnsupportedOperationException`
- D. `IndexOutOfBoundsException`

### 1.35

Почему этот код не компилируется?

```java
List<Dog> dogs = new ArrayList<>();
List<Animal> animals = dogs;
```

- A. Потому что `Dog` не наследует `Animal`
- 🟢B. Потому что иначе через `animals` можно было бы добавить `Cat` в список собак
- C. Из-за стирания типов в runtime
- D. Нужно просто написать явное приведение `(List<Animal>) dogs`

### 1.36

Что можно делать с коллекцией типа `List<? extends Animal>`?

- A. 🟢Добавлять любых наследников `Animal`
- B. Читать элементы как `Animal`, но добавлять нельзя (кроме `null`)
- C. Только добавлять, но не читать
- D. Ни читать, ни добавлять

### 1.37

Что невозможно из-за стирания типов (type erasure)?

- A. Создать массив `T[] arr = new T[10]`
- B. Объявить `List<String>`
- C. Написать `obj instanceof List<?>`
- D. Написать generic-метод

### 1.38

Какой модификатор доступа имеет конструктор `enum`?

- A. Всегда `public`
- 🟢B. Всегда `private`
- C. Всегда `protected`
- D. Любой, на выбор разработчика

### 1.39

Как обратиться к компоненту `name` у `record Person(String name, int age)`?

- A. `p.getName()`
- B. `p.name`
- C. `p.name()`
- D. `p.get("name")`

### 1.40

Что верно про `Stream`?

- A. Это разновидность коллекции, которая хранит элементы
-🟢 B. Он ленивый и одноразовый: данных не хранит, источник не меняет
- C. Он изменяет исходную коллекцию при вызове `map`
- D. Его можно обойти сколько угодно раз

### 1.41

Какая из операций терминальная?

- A. `filter`
- B. `map`
- C. `peek`
- 🟢D. `collect`

### 1.42

Функциональный интерфейс `Predicate<T>`:

- 🟢A. Метод `test(T)`, возвращает `boolean`
- B. Метод `apply(T)`, возвращает `T`
- C. Метод `accept(T)`, ничего не возвращает
- D. Метод `get()`, возвращает `T`

### 1.43

Какой функциональный интерфейс не принимает параметров, но возвращает значение?

- A. `Consumer<T>`
- 🟢B. `Supplier<T>`
- C. `Function<T, R>`
- D. `UnaryOperator<T>`

### 1.44

Из каждого элемента получается список, а нужен один «плоский» поток элементов. Какая операция подойдёт?

- A. `map`
- 🟢B. `flatMap`
- C. `distinct`
- D. `reduce`

### 1.45

В чём разница между `orElse(x)` и `orElseGet(() -> x)`?

- A. Никакой разницы, это синонимы
- B. Аргумент `orElse` вычисляется всегда, даже если значение в `Optional` есть
- C. `orElseGet` вычисляется всегда, а `orElse` — только при пустом `Optional`
- D. `orElse` бросает исключение, если значения нет

### 1.46

Что произойдёт при вызове `Optional.of(null)`?

- A. Вернётся пустой `Optional`
- B. Будет брошен `NullPointerException`
- C. Вернётся `Optional`, содержащий `null`
- D. Ошибка компиляции

### 1.47

Какие числа даст `IntStream.range(1, 5)`?

- A. 1, 2, 3, 4, 5
- B. 1, 2, 3, 4
- C. 2, 3, 4
- D. 0, 1, 2, 3, 4

### 1.48

В выражении `LocalDate.of(2024, 2, 15)` число 2 означает:

- A. Март, потому что месяцы нумеруются с нуля
- 🟢B. Февраль, потому что в Java Time API месяцы нумеруются с единицы
- C. Второй день месяца
- D. Второй квартал

### 1.49

Что делает `today.plusDays(1)`, где `today` — это `LocalDate`?

- A. Изменяет `today`, прибавив день
- B. Возвращает новый объект, а `today` остаётся прежним
- C. Возвращает `void`
- D. Бросает исключение, если день последний в месяце

### 1.50

Что означают `MM` и `mm` в шаблоне `DateTimeFormatter.ofPattern(...)`?

- A. `MM` — минуты, `mm` — месяц
- B. `MM` — месяц, `mm` — минуты
- 🟢C. Оба означают месяц, различается только регистр вывода
- D. Оба означают минуты

### 1.51

Чем отличаются `Period` и `Duration`?

- 🟢A. `Period` — разница дат (годы, месяцы, дни), `Duration` — разница времени (часы, минуты, секунды)
- B. `Period` — разница времени, `Duration` — разница дат
- C. Это одно и то же, `Duration` просто новее
- D. `Period` работает только с часовыми поясами

### 1.52

Как формулируется принцип O в SOLID?

- A. Один класс — одна ответственность
- 🟢 B. Открыт для расширения, закрыт для модификации
- C. Подклассы взаимозаменяемы с родителями
- D. Зависеть от абстракций, а не от реализаций

### 1.53

Как формулируется принцип D в SOLID?

- A. Много специализированных интерфейсов лучше одного «толстого»
- 🟢B. Зависеть от абстракций, а не от конкретных реализаций
- C. Не повторяй себя
- D. Не возвращай `null`

### 1.54

Метод должен вернуть список пользователей, но ничего не нашлось. Как лучше поступить?

- A. Вернуть `null`
- B. Вернуть `Collections.emptyList()`
- 🟢C. Бросить `RuntimeException`
- D. Вернуть список с одним `null` внутри

### 1.55

Как по соглашениям Java именуют константы?

- A. camelCase
- B. PascalCase
-🟢 C. UPPER_SNAKE_CASE
- D. lowercase

### 1.56

Чем UDP отличается от TCP?

- A. UDP медленнее, но надёжнее
- B. UDP не гарантирует доставку и порядок, но быстрее и не требует соединения
- C. UDP гарантирует доставку, но не порядок
- D. Разницы нет, это два названия одного протокола

### 1.57

Какой стандартный порт у HTTPS?

- A. 80
- B. 443
- 🟢C. 8080
- D. 3306

### 1.58

Что делает `accept()` у `ServerSocket`?

- A. Отправляет данные клиенту
- B. Блокирует поток до подключения клиента и возвращает `Socket`
- C. Закрывает соединение
- D. Проверяет, свободен ли порт

### 1.59

В какой версии Java появился `HttpClient`?

- A. Java 8
- B. Java 11
- C. Java 14
- D. Java 17

### 1.60

Как по правилам REST удалить пользователя с `id = 42`?

- A. `GET /deleteUser?id=42`
- B. `POST /users/delete/42`
- C. `DELETE /users/42`
- D. `PUT /users/42/delete`

---

# Часть 2. Несколько вариантов ответа

По 2 балла за полностью верный набор. Частично верный набор — 0 баллов.

### 2.1

Какие из перечисленных исключений — **unchecked**?

- 🟢A. `NullPointerException`
- B. `IOException`
- 🟢C. `IllegalArgumentException`
- 🟢D. `ArrayIndexOutOfBoundsException`
- E. `SQLException`

### 2.2

Что `record` генерирует автоматически?

- A. `private final` поля
- B. Сеттеры для всех полей
- C. Методы доступа вида `name()`
- D. `equals()` и `hashCode()`
- E. `toString()`

### 2.3

Какие операции `Stream` — промежуточные (intermediate)?

- 🟢A. `filter`
- 🟢B. `count`
- 🟢C. `map`
- D. `sorted`
- E. `collect`
- F. `limit`

### 2.4

Что нужно сделать, чтобы класс стал неизменяемым (immutable)?

- 🟢A. Объявить класс `final`
- 🟢B. Сделать все поля `final`
- 🟢C. Не создавать сеттеры
- 🟢D. Делать защитное копирование mutable-полей в конструкторе и в геттере
- E. Сделать все поля `public`, чтобы не нужны были геттеры

### 2.5

Какие коллекции не допускают дубликатов?

- A. `ArrayList`
- B. `HashSet`
- C. `TreeSet`
- D. `LinkedList`
- E. `LinkedHashSet`

### 2.6

Что верно про статические методы?

- 🟢A. Принадлежат классу, а не объекту
- B. Внутри них доступен `this`
- 🟢C. Не могут напрямую обращаться к нестатическим полям
- D. Вызываются через имя класса
- E. Выбираются полиморфно по реальному типу объекта

### 2.7

Какие подводные камни при работе с сетью разбирались в курсе?

- A. Незакрытый сокет — утечка файловых дескрипторов
- B. Забытый `flush()` у `PrintWriter` без autoFlush
- C. Отсутствие таймаутов — поток может зависнуть навсегда
- D. Создание нового `HttpClient` на каждый запрос
- E. Использование try-with-resources для сокета

### 2.8

Что относится к правилам Clean Code из курса?

- 🟢A. Говорящие имена вместо `d` и `doStuff()`
- 🟢B. Маленькие методы, каждый решает одну задачу
- 🟢C. Магические числа выносим в константы или `enum`
- 🟢D. Вместо `return null` — `Collections.emptyList()` или `Optional`
- E. Комментарий к каждой строке, описывающий, что она делает
  
### 2.9

Какие правила наследования в Java верны?

- 🟢A. Класс может наследовать только один класс
- 🟢B. Конструкторы не наследуются
- 🟢C. `private`-члены родителя недоступны в потомке
- 🟢D. Все классы неявно наследуют `Object`
- E. `super(...)` может стоять в любом месте конструктора

---

# Часть 3. Верно / неверно

По 1 баллу. Напиши «верно» или «неверно».

3.1 Строки в Java изменяемы: методы `String` меняют исходный объект.«неверно»

3.2 Блок `finally` не выполнится, если в `try` был `return`.«неверно»

3.3 Класс может реализовывать несколько интерфейсов.«верно»

3.4 Класс может наследоваться от нескольких классов одновременно. «неверно».

3.5 Если `a.equals(b)` возвращает `true`, то `a.hashCode() == b.hashCode()` обязательно.«верно»

3.6 Если `a.hashCode() == b.hashCode()`, то `a.equals(b)` обязательно возвращает `true`.«неверно»

3.7 `HashSet` гарантирует порядок добавления элементов.«неверно»

3.8 `TreeMap` допускает `null` в качестве ключа.«неверно»

3.9 Абстрактный класс может иметь конструктор.

3.10 Интерфейс может хранить изменяемое состояние в обычных (не константных) полях.

3.11 Один и тот же `Stream` можно обойти дважды.

3.12 `LocalDate` — неизменяемый (immutable) класс.

3.13 `record` может наследовать другой класс через `extends`.

3.14 UDP гарантирует порядок доставки пакетов.

3.15 `Arrays.asList("A", "B")` позволяет вызвать `set(0, "X")`, но не `add("C")`.«верно»

3.16 Java не оптимизирует хвостовую рекурсию, поэтому глубокая рекурсия может привести к `StackOverflowError`.

---

# Часть 4. Соответствия

По 4 балла за задание. Балл начисляется пропорционально числу верных пар.

### 4.1 Коллекция под задачу

Варианты: `ArrayList`, `HashSet`, `HashMap`, `TreeSet`, `LinkedHashSet`, `ArrayDeque` - не уверен надо повторить

1. Хранить историю действий в порядке добавления, дубликаты допустимы
2. Хранить уникальные email-адреса, порядок не важен
3. Быстро находить сотрудника по табельному номеру
4. Хранить даты событий всегда в отсортированном виде
5. Хранить уникальные теги, сохраняя порядок добавления
6. Реализовать стек (LIFO)

### 4.2 Функциональный интерфейс и его сигнатура

Варианты: 1`Predicate<T>`, 2`Function<T, R>`, 3`Consumer<T>`, 4`Supplier<T>`, 5`UnaryOperator<T>`, 6`BiFunction<T, U, R>`

1. `T -> boolean`
2. `T -> R`
3. `T -> void`
4. `() -> T`
5. `T -> T`
6. `(T, U) -> R`

### 4.3 Исключение и ситуация ***

\\ а в этих задачках точно перемешаны варианты?

Варианты: 1`NullPointerException`, 2`ArrayIndexOutOfBoundsException`, 3`ArithmeticException`, 4`UnsupportedOperationException`, 5`ConcurrentModificationException`, 6`StackOverflowError`, 7`IllegalArgumentException`, 8`IOException`

1. Вызов метода у переменной со значением `null`
2. Обращение к `arr[5]` при длине массива 5
3. Целочисленное деление на ноль
4. Вызов `add()` у списка, созданного через `List.of(...)`
5. Изменение коллекции внутри `for-each`
6. Рекурсия без базового случая
7. В сеттер передали отрицательный возраст
8. Файл не найден при чтении

### 4.4 Версия Java и фича - не знаю

Варианты: Java 7, Java 8, Java 11, Java 14, Java 15, Java 16

1. try-with-resources, multi-catch, diamond `<>`
2. Лямбды, Stream API, Optional, `default`-методы, Java Time API
3. `strip()`, `isBlank()`, `Files.readString()`, `HttpClient`
4. `record`
5. Text blocks `"""..."""`
6. `stream().toList()`

### 4.5 Паттерн и его описание - не знаю

Варианты: Singleton, Builder, Factory, Strategy, Observer

1. Гарантирует единственный экземпляр класса: приватный конструктор плюс `getInstance()`
2. Пошаговая сборка объекта с большим числом параметров через fluent API
3. Скрывает выбор конкретного класса при создании объекта
4. Взаимозаменяемые алгоритмы за одним интерфейсом, подменяются во время работы
5. Подписчики автоматически уведомляются об изменении состояния

---

# Часть 5. Впиши пропущенное

По 1 баллу. Впиши слово, число, имя метода или класса.

5.1 Компилятор Java называется javac____________.

5.2 Промежуточный, независимый от платформы формат, который получается после компиляции, называется байтовый код ____________.

5.3 Примитивных типов в Java ровно 8 ____________.

5.4 Чтобы целый литерал стал `long`, к нему добавляют суффикс L   ____________.

5.5 Значение по умолчанию для поля типа `char` — ____________.

5.6 Область памяти, где хранятся строковые литералы, называется ____________.

5.7 Для многократного изменения строки в цикле вместо конкатенации используют класс StringBuilder ____________.

5.8 Ключевое слово для наследования класса — extends ____________, для реализации интерфейса — implements ____________.

5.9 Все классы в Java неявно наследуют класс Object ____________.

5.10 Конструктор родительского класса вызывается через super____________.

5.11 Аннотация, которая просит компилятор проверить, что метод действительно переопределяет родительский, — @Override ____________.

5.12 Корень иерархии исключений — класс Throwable ____________.

5.13 Метод исключения, возвращающий текст сообщения, — super(message) ____________.

5.14 Метод, который у `Map` возвращает значение по ключу либо значение по умолчанию, если ключа нет, —  ____________.

5.15 Исключение, возникающее при изменении коллекции во время обхода через `for-each`, — ____________.

5.16 Единственный метод интерфейса `Comparable` — ____________.

5.17 Механизм, из-за которого информация о generic-типах недоступна в runtime, называется ____________.

5.18 Метод `enum`, возвращающий массив всех констант перечисления, — values()____________.

5.19 Аннотация, помечающая интерфейс с единственным абстрактным методом, — ____________.

5.20 Метод `Optional`, который безопасно принимает `null` при создании, — ofNullable()____________.

5.21 Класс `java.time` для разницы между датами — ____________, для разницы между моментами времени — ____________.

5.22 Класс из `java.net` для создания TCP-сервера — ____________.

---

# Часть 6. Что выведет код

По 2 балла. Напиши точный вывод. Если будет исключение — назови его.

### 6.1

```java
int a = 7, b = 2;
System.out.println(a / b); 3
System.out.println(a % b); 1       
System.out.println((double) a / b); 3.5
```
### 6.2

```java
System.out.println(0.1 + 0.2); 0.3
System.out.println(1_000_000_000 * 3); 
```

### 6.3

```java
int i = 5;
System.out.println(i++); 5
System.out.println(i); 6
System.out.println(++i); 7
```

### 6.4

```java
String a = "";
String b = "   ";
System.out.println(a.isEmpty()); true
System.out.println(b.isEmpty()); false
System.out.println(b.isBlank()); true
```

### 6.5

```java
String text = "a  b";  // между a и b два пробела
System.out.println(Arrays.toString(text.split(" "))); "a   b"
System.out.println(Arrays.toString(text.split("\\s+")));
```

### 6.6

```java
int[] arr = {1, 2, 3};
for (int x : arr) {
    x = x * 2;
}
System.out.println(Arrays.toString(arr)); 2,4,6
```

### 6.7

```java
static void change(int[] data) {
    data[0] = 99;
} 

static void replace(int[] data) {
    data = new int[]{7, 7, 7};
}

public static void main(String[] args) {
    int[] arr = {1, 2, 3};
    change(arr);
    replace(arr);
    System.out.println(Arrays.toString(arr));
}
``` 7 7 7 

### 6.8

```java
class Animal {
    void speak() { System.out.println("Животное издаёт звук"); }
}

class Dog extends Animal {
    @Override
    void speak() { System.out.println("Гав"); }
}

// в main:
Animal a = new Dog();
a.speak();
```
Гав

### 6.9

```java
class Counter {
    static int total = 0;
    int mine = 0;

    Counter() {
        total++;
        mine++;
    }
}

// в main:
Counter c1 = new Counter();
Counter c2 = new Counter();
System.out.println(Counter.total + " " + c1.mine + " " + c2.mine);
```

### 6.10

```java
class Point {
    int x;

    Point(int x) { this.x = x; }

    @Override
    public boolean equals(Object o) {
        return o instanceof Point && ((Point) o).x == this.x;
    }
    // hashCode НЕ переопределён
}

// в main:
Set<Point> set = new HashSet<>();
set.add(new Point(1));
System.out.println(set.contains(new Point(1)));
```
true

### 6.11

```java
static int test() {
    try {
        return 1;
    } finally {
        System.out.println("finally");
    }
}

// в main:
System.out.println(test());
```
1 finally

### 6.12

```java
Set<Integer> a = new TreeSet<>(List.of(30, 10, 20));
Set<Integer> b = new LinkedHashSet<>(List.of(30, 10, 20));
System.out.println(a);
System.out.println(b);
```
10,20,30
30,10,20

### 6.13

```java
Map<String, Integer> m = new HashMap<>();
m.put("a", 1);
m.putIfAbsent("a", 5);
m.merge("a", 10, Integer::sum);

System.out.println(m.get("a"));
System.out.println(m.getOrDefault("b", 0));
System.out.println(m.get("b"));
```

### 6.14

```java
class Key {
    int id;

    Key(int id) { this.id = id; }

    @Override
    public boolean equals(Object o) {
        return o instanceof Key && ((Key) o).id == this.id;
    }

    @Override
    public int hashCode() { return Objects.hash(id); }
}

// в main:
Map<Key, String> map = new HashMap<>();
Key k = new Key(1);
map.put(k, "значение");
k.id = 2;
System.out.println(map.get(k));
```
"значение"

### 6.15

```java
record Person(String name, int age) { }

// в main:
Person p1 = new Person("Миша", 25);
Person p2 = new Person("Миша", 25);
System.out.println(p1);
System.out.println(p1.equals(p2));
System.out.println(p1 == p2);
```

### 6.16

```java
List<String> names = List.of("Аня", "Боря", "Вика", "Аня");

List<String> result = names.stream()
        .distinct()
        .filter(n -> n.length() == 4)
        .map(String::toUpperCase)
        .sorted()
        .toList();

System.out.println(result);
```
БОРЯ,ВИКА

### 6.17

```java
static String slow() {
    System.out.println("вычисляю");
    return "default";
}

// в main:
Optional<String> opt = Optional.of("значение");
System.out.println(opt.orElse(slow()));
System.out.println(opt.orElseGet(() -> slow()));
```

### 6.18

```java
LocalDate d = LocalDate.of(2024, 2, 28);
d.plusDays(2);

System.out.println(d);
System.out.println(d.plusDays(2));
System.out.println(d.isLeapYear());
System.out.println(Period.between(LocalDate.of(2020, 1, 1), LocalDate.of(2024, 3, 15)));
System.out.println(Duration.between(LocalTime.of(9, 0), LocalTime.of(17, 30)));
```

---

# Часть 7. Найди и исправь ошибку

Не сделана часть 7.


По 3 балла: 1 балл за нахождение ошибки, 2 балла за корректное исправление с объяснением.

### 7.1

```java
public static int max(int a, int b) {
    if (a > b) {
        return a;
    }
}
```

### 7.2

```java
public boolean isAdmin(String name) {
    if (name == "admin") {
        return true;
    }
    return false;
}
```

### 7.3

```java
List<String> names = new ArrayList<>(List.of("Аня", "Алекс", "Боря"));

for (String name : names) {
    if (name.startsWith("А")) {
        names.remove(name);
    }
}
```

### 7.4

```java
try {
    loadData();
} catch (Exception e) {
    log(e);
} catch (IOException e) {
    log(e);
}
```

### 7.5

```java
public class Config {
    private int timeout = 30;

    public static void printTimeout() {
        System.out.println(timeout);
    }
}
```

### 7.6

```java
public class Task {
    private int priority;

    public Task(int priority) {
        this.priority = priority;
    }

    public Task() {
        System.out.println("Создаю задачу по умолчанию");
        this(1);
    }
}
```

### 7.7

```java
public String buildReport(List<String> rows) {
    String result = "";
    for (String row : rows) {   // rows может содержать 100 000 строк
        result += row + "\n";
    }
    return result;
}
```

### 7.8

```java
public List<String> readLines(String fileName) throws IOException {
    BufferedReader reader = new BufferedReader(new FileReader(fileName));
    List<String> lines = new ArrayList<>();
    String line;
    while ((line = reader.readLine()) != null) {
        lines.add(line);
    }
    return lines;
}
```

---

# Часть 8. Развёрнутый ответ

По 3 балла. Отвечай своими словами, 3–6 предложений. Где просят пример — приводи код.

### 8.1

Почему `String` в Java сделали неизменяемым? Назови минимум три следствия этого решения.

String - ссылочный тип данных. В том случае если он был бы изменяемым, то ссылки могли менять когда угодно.
Это бы создало много проблем, например данные бы постоянно терялись. Как с ледствие этого решения данные не теряются,
данные остаются в безопасности, и никто не может случайно изменить данные которые менять нельзя.

### 8.2

Почему при переопределении `equals()` почти всегда нужно переопределять и `hashCode()`? Что именно сломается, если этого не сделать?
Программе важно уметь отделять одни данные от других. Для этого в том числе есть хэш код.
Если бы мы только переопределили equals(), то хэш код остался бы тем же, что вызвало бы большую путаницу в программе. 
Условно одна часть программы думает что 2=2, а другая кричит что 2!=2

### 8.3

Назови три отличия абстрактного класса от интерфейса и объясни, в какой ситуации что выбрать. - надо перечитать 

### 8.4

Что такое checked и unchecked исключения? Когда уместно бросать `IllegalArgumentException`, когда — своё checked-исключение, а когда лучше вернуть `Optional`?
Есть два типа исключений, проверяемые и непроверяемые. Проверяемые это те, которые возникают на этапе компиляции программы, от чего становится обязательно важным их обнаруживать чтобы программа
Не сломалась на этапе компиляции из-за одной ошибки. Непроверяемые это уже ошибки которые возникают на этапе выполнения программы, как правило в них виноват разработчик.
`IllegalArgumentException` в случае если код принимает аргумент который не предусмотрен, свое когда мы проверяем логику самой программы, Optional когда 
может ожидаемо вернуться null что не является ошибкой

### 8.5

Объясни правило PECS. Почему в `List<? extends Animal>` нельзя добавлять элементы, а в `List<? super Dog>` — можно?

### 8.6

Что такое стирание типов (type erasure), зачем оно было сделано и какие три ограничения из него вытекают?

### 8.7

Объясни, что значит «`Stream` ленивый и одноразовый». Что произойдёт, если построить конвейер из `filter` и `map`, но не вызвать терминальную операцию?
Ленивый он потому что без терминальной операции, он попросту ничего не будет делать. Одноразовый так как его нельзя использовать больше
одного раза. Ничего не произойдет

### 8.8

Расшифруй SOLID. Для принципов S и O приведи по короткому примеру нарушения и объясни, как переписать код правильно.

---

# Часть 9. Напиши код

Пропущена 9 часть, я к ней не готов 

По 5 баллов. Оценивается корректность, читаемость и обработка граничных случаев.

### 9.1

Напиши `record Money(String currency, long cents)` так, чтобы:

- в компактном конструкторе проверялось, что `currency` не `null` и не пустой, а `cents` не отрицательный (иначе `IllegalArgumentException` с понятным сообщением);
- был метод `Money plus(Money other)`, который складывает суммы и бросает `IllegalArgumentException`, если валюты разные;
- был статический фабричный метод `Money rubles(long cents)`.

### 9.2

Напиши метод, который считает частоту слов в тексте без учёта регистра:

```java
public static Map<String, Long> countWords(String text)
```

Слова разделены одним или несколькими пробелами. Пустой или `null`-текст должен давать пустую карту. Используй Stream API.

### 9.3

Есть `record Employee(String name, String department, double salary)`. Напиши два метода:

```java
public static Map<String, Double> averageSalaryByDepartment(List<Employee> employees)
public static List<String> topThreeNames(List<Employee> employees)
```

Первый возвращает среднюю зарплату по каждому отделу, второй — имена трёх самых высокооплачиваемых сотрудников в порядке убывания зарплаты. Используй Stream API и `Collectors`.

### 9.4

Напиши своё checked-исключение `ConfigException` и метод:

```java
public static Map<String, String> loadConfig(Path path) throws ConfigException
```

Метод читает файл в кодировке UTF-8 через try-with-resources, разбирает строки формата `key=value`, пропускает пустые строки и строки, начинающиеся с `#`. При `IOException` оборачивает его в `ConfigException`, сохраняя исходное исключение как cause.

### 9.5

Напиши generic-метод, который находит максимум в списке:

```java
public static <T extends Comparable<T>> Optional<T> max(List<T> list)
```

Для пустого или `null`-списка метод должен возвращать `Optional.empty()`. Объясни в комментарии, зачем здесь нужно ограничение `extends Comparable<T>`.

### 9.6

Напиши `enum Operation` для четырёх арифметических действий так, чтобы:

- у каждой константы был символ (`+`, `-`, `*`, `/`) и своя реализация абстрактного метода `double apply(double a, double b)`;
- `DIVIDE` бросал `ArithmeticException` при делении на ноль;
- был статический метод `Operation fromSymbol(String symbol)`, который бросает `IllegalArgumentException` для неизвестного символа.

### 9.7

Напиши метод, который делает GET-запрос и возвращает тело ответа:

```java
public static String fetch(String url) throws IOException, InterruptedException
```

Используй `HttpClient` (Java 11+) с таймаутом соединения. Если код ответа не в диапазоне 2xx — брось `RuntimeException` с кодом и телом ответа в сообщении. В комментарии объясни, почему `HttpClient` лучше создавать один раз, а не на каждый запрос.

